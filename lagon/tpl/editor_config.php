<?php if (!defined('DATATABLES')) exit();
// Lagon7 DEV -> extranet/include/Editor-PHP-1.6.5/php/config.php (généré via envsubst).

error_reporting(E_ALL);
ini_set('display_errors', '1');

$sql_details = array(
    "type" => "Mysql",
    "user" => "${LAGON_DB_USER}",
    "pass" => "${LAGON_DB_PASS}",
    "host" => "${LAGON_DB_HOST}",
    "port" => "",
    "db"   => "${LAGON_DB_NAME}",
    "dsn"  => "",
    "pdoAttr" => array()
);

# Secrets base partagée du pôle assurmix (dev) — copie en secrets.env.
# Identifiants du conteneur portomix_data (MySQL 5.7) — MÊMES valeurs que
# portomix/assurmix/crmix (base partagée). (secrets.env gitignoré)
vault_portomix_db_user="innodev"
vault_portomix_db_password="CHANGE_ME"
vault_portomix_db_root_password="CHANGE_ME"
